# Python dependencies for Raspberry Pi 5.
#
# Unlike Jetson (see ../jetson/requirements.txt), RPi5 has no GPU/CUDA
# acceleration for ONNX Runtime — everything runs on CPUExecutionProvider,
# so all packages below install cleanly from standard PyPI. No custom
# wheels needed.

numpy
onnx
onnxruntime
opencv-python-headless
Pillow
protobuf
psutil
picamera2
PyYAML
requests
tqdm
ultralytics
